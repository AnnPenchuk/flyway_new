[environments.target]
url = "jdbc:postgresql://gp_target:5432/target_dev"
user = "admin"
password = "password"
locations = [
    "filesystem:/flyway/sql/target/schemas"
]

[flyway]
environment = "target"
group = true
